#include <stdio.h>
#include <math.h>

int main()
{
	int a = 100;
	for (a = 100; a <= 200; a++)
	{
		int b = 2;
		for (b = 2; b<a; b++)
		{
			if (a%b == 0)
				break;
		}
		if (b >= a)
			printf("%d ", a);
	}
	printf("\n");
	system("pause");
	return 0;
}

#include <stdio.h>
#include <math.h>
int main()
{
int i = 1;
float j=0.0, k=0.0;
for (i = 1; i < 100; i++)
     {
	      j = pow(-1, i + 1);
          k = k + j * 1 / i;
      }
          printf("%f", k);
		  system("pause");
          return 0;
}

#include <stdio.h>
int main()
{
	int a[10], i = 0,max = a[0];
	for (i=0; i < 10; i++)
		{
			scanf("%d ", &a[i]);
		        if (a[i] > max)
				max = a[i];
		}
	printf("max=%d\n", max);
	system("pause");
	return 0;
}

#include <stdio.h>
int main()
{
	int a, b, c,i;
	scanf_s("%d %d %d", &a, &b, &c);
	if (a < b)
		i = a; a = b; b = i;
	if (b < c)
		i = b; b = c; c = i;
	printf("%d %d %d", a, b, c);
	system("pause");
	return 0;
}
